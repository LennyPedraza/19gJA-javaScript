/**
 * Repaso metodos de arraw functions
 * -> parametros
 * ->invocacion de funciones
 */
// Declaracion de la funcion
const suma = (a = 0 ,b = 0) => {
    // sentencias
    let total = a+b
    return total
}

// invocar
// suma() -> sin argumentos
// suma(2,3) -> con argumentos
suma()
//0
suma(1,2)
arrMultiple.map(callback)
arrMultiple.map(function(cv,index,arr) {})
arr.map( () => {})


/**
 * Repaso metodos de array
 */